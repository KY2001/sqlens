[project]
name = "sqlens"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sqlfluff>=4.2.2",
    "sqruff>=0.39.0",
    "ruff>=0.15.20",
    "ty>=0.0.56",
]

[tool.uv]
compile-bytecode = true
